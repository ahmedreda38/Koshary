# Koshary secrets. Copy to .env and fill in.
# .env is gitignored. These values are redacted from all logs.

# CTFd session cookie (CTFd mode)
CTFD_SESSION=

# Hack The Box CTF MCP token (htb_ctf mode)
HTB_MCP_TOKEN=
